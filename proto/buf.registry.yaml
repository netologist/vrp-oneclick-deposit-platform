version: v2
name: buf.build/netologist/vrp-platform
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    - UNARY_RPC
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
    - WIRE_JSON
